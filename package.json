{
  "name": "scanner",
  "version": "1.0.0",
  "private": true,
  "main": "scanner.js",
  "bin": "scanner",
  "scripts": {
    "start": "node scanner.js",
    "build": "pkg scanner.js --targets node18-linux-x64 --output scanner"
  },
  "pkg": {
    "assets": [
      "pack.json",
      "site/**/*"
    ]
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1063.0",
    "axios": "^1.7.0",
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "pkg": "^5.8.1"
  }
}
